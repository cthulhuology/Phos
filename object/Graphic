An.object().named('Graphic').plural('Graphics').from(Widget,{
	r: 255, g: 255, b: 255,
	color: function(r,g,b) {
		this.r = Math.min(255,Math.max(0,1*r)); 
		this.g = Math.min(255,Math.max(0,1*g));
		this.b = Math.min(255,Math.max(0,1*b));
		return this;
	},
});
